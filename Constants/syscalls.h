
#pragma once

enum Syscalls {
	READ = 0,
	WRITE = 1,
	GETPID = 39,
	EXECVE = 59,
	KILL = 62,
	PROCCOUNT,
	GETPROCS,
	GETTIME,
	GETREGISTERS,
	MICROSLEEP,
	MILLIS,
	SETGRAPHIC,
	DRAWFIGURE,
	SETFOREGROUND,
	GETWINDOWINFO,
	DRAWBITMAP,
	FLIP,
	SWITCHTODESKTOP,
};

enum Figures {
	CIRCLE = 0,
	RECTANGLE,
	TEXT,
	LINE,
};

#define EOF -1
#define TIMEOUT -2
